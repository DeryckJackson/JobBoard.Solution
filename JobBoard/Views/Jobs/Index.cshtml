<div class="container">
  @using JobBoard.Models;
  <h1>To Do List</h1>
  @if (Model.Count == 0)
  {
    <p>There are no Jobs.</p>
  }
  <ul>
    @foreach (Job job in Model)
    {
      <li><a href="jobs/@job.Id">@job.GetTitle()</a></li><br>
      
    }
  </ul>
  <a href="/jobs/new">Add a new item.</a>
